# Type of solver to get transport map
solver_type: "sinkhorn"

# Sinkhorn solver settings
relative_epsilon: 0.0005
max_iterations: 10000
tau_a: 1.0
tau_b: 1.0
low_rank: false
rank: "auto"
verbose: true
progress_bar: true
disable_tqdm: true