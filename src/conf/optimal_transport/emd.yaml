# Type of solver to get transport map
solver_type: "emd" # "emd" or "sinkhorn"

# Progress
verbose: false
disable_tqdm: true

# For saving model in sweep. Not relevant to algorithm.
tau: 1
epsilon: 0 # For saving model name