program: src/main.py
method: grid

parameters:
  
  fused_models_dir:
   values:
    - "../../../models/optimization_models/"

  models_conf_dir: 
   values:
    - "../../../src/conf/models/optimization_models/"

  optimal_transport:
    value:
      - sinkhorn

  optimal_transport.epsilon:
      # TODO: Check in code for relative vs absolute epsilon
      values:
        - 0.1
        - 0.01
        - 1e-6


  graph_cost:
    values:
      - feature_lp
      - fused_gw
      - quadratic_energy

      
command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}